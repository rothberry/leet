/**
 * @param {number} n
 * @return {string[]}
 */
const fizzBuzz = (n) => {

    /*
        create an output array
        loop n times
        for each loop
            if i is divisble by 15
                push "FizzBuzz"
            else if i divis by 3
                push "Fizz"
            else if i is divis by 5
                push "Buzz"
            else 
                push i.toString
        
        return output array

    //    ! ORRRRR

        create an output array
        loop n times
        for each loop
            create var that we will push into output array
            if n is divisible by 3
                var = "Fizz"
            if (no else) n i divis by 5
                ADD "Buzz" to var
            else (or not else depending on langauage)
                var = n.toString
            push var into output

        return output array
    
    */
}
